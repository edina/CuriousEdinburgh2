import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    scrollView: {
        position: 'absolute',
        height: 60,
        left: 0,
    },
    image: {
        backgroundColor: '#098caa',
        color: '#ffffff',
        minHeight: 60,
        padding: 5,
    },
});
