import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    scrollView: {
        position: 'absolute',
        height: 200,
        left: 0,
    },
    image: {
        backgroundColor: '#268da8',
        color: '#ffffff',
        padding: 5,
        textAlign: 'center',
    },
});
